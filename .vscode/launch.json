{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "yarn run dev",
      "name": "Run dev",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}
